<!DOCTYPE html>
<html>
  <head>
    <title>Tixi Renovations Melbourne | <%= yield(:title) %></title>
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%= render partial: 'layouts/header' %>
    <header>
      <%= link_to image_tag("logo.jpg", id: "logo",
        alt: "Tixi Renovations Melbourne"), root_url %>
    </header>
    <nav>
      <% if nav_pages %>
        <ul>
          <% nav_pages.each do |page| %>
            <li class="<%= 'active' if params[:id] == page.slug %>">
              <%= link_to page.name, root_url + page.slug %>
            </li>
          <% end %>
          <li class="<%= 'active' if (params[:controller] == 'photos' &&
            params[:action] == 'gallery') %>">
            <%= link_to "Gallery", gallery_path %>
          </li>
          <li class="<%= 'active' if (params[:controller] == 'enquiries' &&
            params[:action] == 'new') %>">
            <%= link_to "Enquiry", new_enquiry_path %>
          </li>
        </ul>
      <% end %>
    </nav>
    <main>
      <% unless yield(:title) == 'Home' %>
        <h2><%= yield(:title) %></h2>
      <% end %>
      <%= render partial: 'layouts/flash_messages' %>
      <%= yield %>
    </main>
    <footer>
      <%= render partial: 'layouts/footer' %>
    </footer>
  </body>
</html>
